This error occurs when you try to access a state variable or prop before it has been initialized or updated.  This frequently happens inside a component's render method, before `useEffect` hooks or async operations have completed.